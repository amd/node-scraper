{
  "global_args": {},
  "plugins": {
    "GenericCollectionPlugin": {
      "collection_args": {
        "commands": [
          "uname -s",
          "uname -m",
          "hostname",
          {
            "command": "echo generic-collection-ok",
            "timeout": 60
          },
          {
            "command": "id -u",
            "sudo": false
          },
          "test -d /proc && echo proc-ok"
        ]
      }
    }
  },
  "result_collators": {},
  "name": "GenericCollectionPlugin config",
  "desc": "Config for testing GenericCollectionPlugin with plain and per-command options"
}
